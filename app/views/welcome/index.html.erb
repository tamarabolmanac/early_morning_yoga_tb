<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <meta name="turbo-visit-control" content="reload">
  <title>Early Morning Yoga</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@700&family=Raleway:wght@300;400;600&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="styles.css">
</head>
<body>

  <!-- Flash message -->
  <% if flash[:notice] %>
    <div class="flash-message notice">
      <%= flash[:notice] %>
    </div>
  <% end %>

  <!-- Header Section -->
  <header class="header">

    <div class="overlay">
      <h1>Welcome to Early Morning Yoga</h1>
      <p class="welcome-text">Your peaceful start to a healthy day</p>
    </div>

    <!-- Login Button -->
    <% if !session[:current_user_id] %>
      <a href="/session/new" class="login-btn">Login</a>
    <% else %>
      <%= button_to logout_path, method: :delete, class: 'login-btn' do %>
        Sign out
      <% end %>
    <% end %>
  </header>

  <!-- Main Content -->
  <main class="main-content">
    <section class="about-yoga card">
      <h2><a href="/about-yoga" class="section-link">About Yoga</a></h2>
      <p>Yoga is a practice that aligns the body, mind, and spirit. It is a transformative journey that cultivates inner peace, flexibility, strength, and balance...</p>
    </section>

    <section class="health card">
      <h2><a href="#" class="section-link">Living a Healthy Life</a></h2>
      <p>Living a healthy life involves nourishing your body, mind, and soul...</p>
    </section>

    <section class="routine card">
      <h2><a href="#" class="section-link">Daily Routines</a></h2>
      <p>Start each day with intention...</p>
    </section>
  </main>

  <!-- Footer Section -->
  <footer class="footer">
    <p>© 2024 Early Morning Yoga. All rights reserved.</p>
  </footer>

  <!-- JavaScript for Hamburger Menu -->
  <script>
    function toggleMenu() {
      const menu = document.querySelector('.menu');
      if (menu) {
        menu.classList.toggle('show-menu');
      }
    }

    // Close the menu if clicking outside
    window.addEventListener('click', function (event) {
      const menu = document.querySelector('.menu');
      const menuToggle = document.querySelector('.menu-toggle');
      if (menu && !menu.contains(event.target) && !menuToggle.contains(event.target)) {
        menu.classList.remove('show-menu');
      }
    });
  </script>


</body>
</html>
